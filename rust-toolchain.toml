[toolchain]
# M-A2: pin to the same channel as the rest of the workspace (1.96.0).
# MSRV 1.96.0 is enforced by CI (the floor is the pinned channel — idle-api
# is edition-2024, so anything older cannot resolve the workspace).
channel = "1.96.0"